apiVersion: v1
kind: Service
metadata:
  name: department-service
spec:
  selector:
    app: department-service
  ports:
    - protocol: TCP
      port: 9001
      targetPort: 9001
  type: ClusterIP
